[package]
name = "swal-store"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Store trait: rusqlite (native) / IndexedDB (web) backends, shared serde schema"

[dependencies]
