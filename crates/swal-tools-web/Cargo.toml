[package]
name = "swal-tools-web"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Web tools: git→isomorphic-git, fs→OPFS, shell→WebContainers (wasm32)"

[dependencies]
