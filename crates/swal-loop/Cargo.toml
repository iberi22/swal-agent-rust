[package]
name = "swal-loop"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Conversational agent loop: prompt → LLM → tool_calls → execute → feedback"

[dependencies]
