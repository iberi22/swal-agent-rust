#[allow(dead_code)]
pub struct XavierClient;

impl XavierClient {
    #[allow(dead_code)]
    pub fn new(_base: &str) -> Self {
        Self
    }

    #[allow(dead_code)]
    pub async fn search(&self, _q: &str) -> Vec<String> {
        Vec::new()
    }
}
